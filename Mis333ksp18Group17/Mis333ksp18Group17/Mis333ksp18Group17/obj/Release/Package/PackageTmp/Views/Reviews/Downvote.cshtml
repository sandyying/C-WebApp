@model Mis333ksp18Group17.Models.Review
@using Mis333ksp18Group17.Controllers
@{
    ViewBag.Title = "Downvote";
}

<h2>Downvote</h2>



@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Downvote</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ReviewID)

        @*<div class="form-group">
                @Html.LabelFor(model => model.CheckOutStatus, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    <div class="checkbox">
                        @Html.EditorFor(model => model.CheckOutStatus)
                        @Html.ValidationMessageFor(model => model.CheckOutStatus, "", new { @class = "text-danger" })
                    </div>
                </div>
            </div>*@
        <div class="form-group">
            <label class="radio">@Html.RadioButton("Confirm", Confirm.Yes, true) Yes</label>
            <label class="radio">@Html.RadioButton("Confirm", Confirm.No) No</label>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Approved")
</div>
