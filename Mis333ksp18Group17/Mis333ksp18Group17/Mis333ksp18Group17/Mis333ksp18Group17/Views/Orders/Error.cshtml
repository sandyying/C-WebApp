@model IEnumerable<string>

@{
    ViewBag.Title = "Error";
}

<h2>Error</h2>
@if (ViewBag.Error == "Insufficient Popcorn Points")
{
    <h3 style="color: red"> @ViewBag.Error</h3>
}
@Html.ActionLink("OK", "Index", null, new { @class = "btn btn-success" })
